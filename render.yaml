# Connect this repo in Render: New → Blueprint (or New → Static Site)
services:
  - type: web
    name: supercat-month-end
    runtime: static
    plan: free
    buildCommand: "true"
    staticPublishPath: .
    autoDeploy: true
